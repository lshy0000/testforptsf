<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.wheatek.smartdevice">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.READ_PRIVILEGED_PHONE_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:networkSecurityConfig="@xml/network_security_config"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.SmartDevice">
        <activity
            android:name=".SimpleMainActivity"
            android:configChanges="orientation|keyboardHidden|screenSize"
            android:label="@string/app_name"
            android:screenOrientation="landscape"
            android:theme="@style/Theme.SmartDevice.Fullscreen">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".PortraitWebActivity"
            android:screenOrientation="portrait">

        </activity>
        <activity
            android:name=".LandscapeWebActivity"
            android:screenOrientation="landscape">

        </activity>
        <activity
            android:name=".TestActivity"
            android:screenOrientation="portrait">

        </activity>
        <activity
            android:name=".uiv2.ShopActivity"
            android:screenOrientation="landscape"
            android:theme="@style/Theme.SmartDevice.Fullscreen"
            android:label="@string/app_name">


        </activity>
    </application>

</manifest>